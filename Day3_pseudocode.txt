Question -> 1.

Define class Customer:
    - name
    - address

Define class Restaurant:
    - name
    - location

Define class DeliveryBoy:
    - name
    - phone

Define class Order:
    - customer (type: Customer)
    - restaurant (type: Restaurant)
    - deliveryBoy (type: DeliveryBoy)

    Constructor(c, r, d):
        Set customer = c
        Set restaurant = r
        Set deliveryBoy = d

    Method showOrderDetails():
        Print "Customer: " + customer.name
        Print "Restaurant: " + restaurant.name
        Print "Delivery By: " + deliveryBoy.name

Main:
    Create an Order with:
        - Customer: Ravi, Delhi
        - Restaurant: SpiceHub, Connaught Place
        - DeliveryBoy: Amit, 9876543210

    Call order.showOrderDetails()



Question -> 2.
  Define class Patient:
    - name
    - age
    - disease

Create a map called records (key: patient ID, value: Patient)

Add entries:
    - "P101" → Patient("Ravi", 45, "Diabetes")
    - "P102" → Patient("Priya", 30, "Flu")

Retrieve patient with ID "P101" and store in variable p

Print p.name  

Update the record for "P101":
    - Set new value: Patient("Ravi", 45, "Recovered")

Remove the record with key "P102"



Question -> 3.


Define class Book:
    - title
    - year
    - rating

Create a list of books:
    - Book A → year: 2016, rating: 4.6
    - Book B → year: 2014, rating: 4.8
    - Book C → year: 2018, rating: 4.9

Filter the list to include only books where:
    - year > 2015
    - rating > 4.5

Store the filtered books in a new list

For each book in the filtered list:
    Print book.title

